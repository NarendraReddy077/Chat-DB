# 🦜 ChatDB — Chat with Your SQL Database

ChatDB is a Streamlit app that lets you talk to your databases in natural language using Google Gemini.
It automatically converts your question into SQL, executes it, and shows results beautifully.

## 🚀 Features
- Supports SQLite and MySQL
- Auto SQL generation with Gemini
- Schema viewer and downloader
- Question history & conversation reset

## 🧰 Setup
```bash
git clone https://github.com/yourusername/chatdb.git
cd chatdb
pip install -r requirements.txt
streamlit run app.py
